              <% if post.favorite_by(current_user).present? %>
                <%= link_to "お気に入りを取り消す", post_favorite_path(post.id, post.favorite_by(current_user)), method: :DELETE, remote: true, class: "favorite hide-text" %>
              <% else %>
                <%= link_to "お気に入り", post_favorites_path(post), method: :POST, remote: true, class: "star hide-text" %>
              <% end %>